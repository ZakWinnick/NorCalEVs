---
import { navigation } from "../data/navigation";
---

<nav class="site-nav">
  <div class="container site-nav-inner">
    <a href="/" class="site-nav-brand">
      <img src="/assets/images/logo-400.png" alt="NorCal EVs" width="46" height="46" />
      <span>NorCal EVs</span>
    </a>

    <button class="site-nav-toggle" aria-label="Open menu" aria-expanded="false" aria-controls="nav-menu">
      <span></span><span></span><span></span>
    </button>

    <div class="site-nav-menu" id="nav-menu">
      <ul class="site-nav-links">
        {navigation.map((item) =>
          item.submenu ? (
            <li class="site-nav-has-sub">
              <button
                type="button"
                class="site-nav-submenu-toggle"
                aria-expanded="false"
                aria-controls={`submenu-${item.title.toLowerCase()}`}
              >
                {item.title} <span aria-hidden="true">▾</span>
              </button>
              <ul class="site-nav-submenu" id={`submenu-${item.title.toLowerCase()}`}>
                {item.submenu.map((sub) => (
                  <li>
                    <a href={sub.url} target="_blank" rel="noopener noreferrer">
                      {sub.title} <span class="ext-arrow" aria-hidden="true">↗</span>
                    </a>
                  </li>
                ))}
              </ul>
            </li>
          ) : (
            <li>
              <a href={item.url} {...(item.external ? { target: "_blank", rel: "noopener noreferrer" } : {})}>
                {item.title}
                {item.external && <span class="ext-arrow" aria-hidden="true">↗</span>}
              </a>
            </li>
          )
        )}
      </ul>
      <a href="/membership" class="site-nav-join">Join the Community</a>
    </div>
  </div>
</nav>

<script>
  const toggle = document.querySelector(".site-nav-toggle");
  const menu = document.getElementById("nav-menu");
  toggle?.addEventListener("click", () => {
    const open = menu?.classList.toggle("open");
    toggle.setAttribute("aria-expanded", String(!!open));
  });

  document.querySelectorAll(".site-nav-submenu-toggle").forEach((submenuToggle) => {
    submenuToggle.addEventListener("click", () => {
      const parent = submenuToggle.closest(".site-nav-has-sub");
      const open = parent?.classList.toggle("submenu-open");
      submenuToggle.setAttribute("aria-expanded", String(!!open));
    });
  });
</script>
